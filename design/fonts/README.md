# Fonts (optional local preview)

The live design kit loads **Space Grotesk** (headings) + **Inter** (body) from Google Fonts CDN.

App ships the same pairing from `app/src/main/res/font/` (OFL).

Do **not** put proprietary OnePlus Sans here.
r