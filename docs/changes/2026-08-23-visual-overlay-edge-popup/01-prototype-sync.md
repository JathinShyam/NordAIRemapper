# Design sync — Visual Overlay screen

## Why
Compose Visual Overlay screen was rebuilt (left Plus Key edge popup, glow). Prototype updated for parity; live preview box removed later (see `02-remove-live-preview.md`).

## What
| File | Change |
|------|--------|
| `design/nord-edge-prototype.html` | Pixel-parity Visual Overlay: StatusChip, SettingsGroupCard, SettingsToggleRow (icon boxes), PhoneEdgeMock SVG, preview geometry (21.87% key Y, 72% phone), style cards, accent row, hold slider, ghost button |

## Verify
Open `design/nord-edge-prototype.html` → Settings → Visual Overlay. Compare with app: no live preview box; Style cards + Preview on screen remain.
