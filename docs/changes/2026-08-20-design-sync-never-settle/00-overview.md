# Design sync + exact Never Settle lockup

## Why

HTML prototype lagged Compose. Home silhouette Never Settle was a system-font approximation — user required exact mark + motto match.

## What

| File | Change |
|------|--------|
| `PhoneDiagram.kt` + `never_settle_lockup.png` | Exact lockup asset (authentic 1+ + USPTO boxed NEVER/SETTLE) |
| `design/nord-edge-prototype.html` | Pads/hub accents + same lockup image |
| `Color.kt` | Torch Red `#EB0029` |

See also `01-exact-lockup.md`.

## Verify

Home silhouette + prototype Home/Settings match Compose. Compile debug Kotlin.
